/*
 * @author: Rahul
 * @desc: Defines the model that represents the photo that is going to be
 * displayed.
 */
define([
    'backbone'
  ], function(Backbone) {
    return Backbone.Model.extend({ });
  }
);
